// `HeadlessApp` was the pages-router `_app.tsx` wrapper and is removed in v2. For the app
// router, compose the providers in your root `layout.tsx` — see `HeadstartWPApp` in
// `@headstartwp/next/app`.
export * from './Yoast';
export * from './ImageComponent';
